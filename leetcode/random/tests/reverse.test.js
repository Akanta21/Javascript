import { reverse } from '../reverse'

describe('test maxAreaOfIsland', () => {
  test('reverse', () => {
    expect(reverse(120)).toEqual(21)
  })

  test('reverse', () => {
    expect(reverse(2014)).toEqual(4102)
  })

  test('reverse', () => {
    expect(reverse(-233)).toEqual(-332)
  })
})
